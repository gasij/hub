# Система управления заявками - Frontend

React приложение для системы управления заявками, работающее с .NET Core Web API бэкендом.

## Возможности

- 🔐 Аутентификация и авторизация пользователей
- 📝 Создание и управление заявками
- 👥 Роли пользователей (студент/администратор)
- 📊 Дашборд с статистикой заявок
- 🎨 Современный и адаптивный UI

## Технологии

- React 18
- React Router DOM
- Axios для HTTP запросов
- CSS3 с современными стилями
- Context API для управления состоянием

## Установка и запуск

1. Убедитесь, что у вас установлен Node.js (версия 14 или выше)

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение в режиме разработки:
```bash
npm start
```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## Настройка

Приложение настроено для работы с бэкендом на `http://localhost:5000`. Если ваш бэкенд работает на другом порту, измените `API_BASE_URL` в файле `src/services/api.js`.

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Auth.css        # Стили аутентификации
│   ├── Dashboard.css   # Стили дашборда
│   ├── Navbar.css      # Стили навигации
│   ├── TicketList.css  # Стили списка заявок
│   ├── TicketForm.css  # Стили формы заявки
│   ├── TicketDetails.css # Стили детального просмотра
│   ├── Login.js        # Компонент входа
│   ├── Register.js     # Компонент регистрации
│   ├── Dashboard.js    # Главная страница
│   ├── Navbar.js       # Навигационная панель
│   ├── TicketList.js   # Список заявок
│   ├── TicketCard.js   # Карточка заявки
│   ├── TicketForm.js   # Форма создания заявки
│   ├── TicketDetails.js # Детальный просмотр заявки
│   └── ProtectedRoute.js # Защищенный маршрут
├── contexts/           # React контексты
│   └── AuthContext.js  # Контекст аутентификации
├── services/           # API сервисы
│   ├── api.js          # Базовый API клиент
│   ├── authService.js  # Сервис аутентификации
│   ├── ticketService.js # Сервис заявок
│   └── userService.js  # Сервис пользователей
├── App.js             # Главный компонент
├── App.css            # Глобальные стили
└── index.js           # Точка входа
```

## API Endpoints

Приложение работает со следующими эндпоинтами бэкенда:

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему

### Заявки
- `GET /api/tickets` - Получение списка заявок
- `GET /api/tickets/{id}` - Получение заявки по ID
- `POST /api/tickets` - Создание новой заявки
- `PATCH /api/tickets/{id}/status` - Обновление статуса заявки (только для админов)

### Пользователи
- `GET /api/users/search` - Поиск пользователей (только для админов)

## Роли пользователей

- **Студент**: может создавать заявки и просматривать только свои заявки
- **Администратор**: может просматривать все заявки и изменять их статусы

## Статусы заявок

- `new` - Новая
- `in_progress` - В работе
- `resolved` - Решена
- `rejected` - Отклонена
- `closed` - Закрыта

## Сборка для продакшена

```bash
npm run build
```

Создает оптимизированную сборку в папке `build/`.

## Тестирование

```bash
npm test
```

Запускает тесты в интерактивном режиме.