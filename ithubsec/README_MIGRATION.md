# Инструкция по применению миграции

## Проблема
Столбец `DocumentId` отсутствует в таблице `Messages`, из-за чего документы не отправляются пользователям в сообщениях.

## Решение

### Вариант 1: Через Entity Framework (рекомендуется)

1. **Остановите приложение** (закройте процесс `ithubsec.exe`)

2. Выполните команду:
   ```bash
   cd d:\cdv\hub\ithubsec\ithubsec
   dotnet ef database update
   ```

3. Перезапустите приложение

### Вариант 2: Через SQL скрипт

Если не можете остановить приложение, выполните SQL скрипт напрямую в базе данных:

1. Откройте файл `apply_migration.sql` в вашем SQL клиенте (pgAdmin, DBeaver, etc.)

2. Или выполните через командную строку (если установлен PostgreSQL):
   ```bash
   psql -h localhost -U postgres -d hubsec -f apply_migration.sql
   ```

3. Введите пароль: `sl213ON009`

### Проверка

После применения миграции проверьте:
1. Столбец `DocumentId` должен появиться в таблице `Messages`
2. При создании новой заявки должно автоматически создаваться сообщение с документом
3. В интерфейсе сообщений должна появиться кнопка "📥 Скачать документ"

